import 'dart:io';
factorial(int num)
{
 int fac = 1;
 for(int i = num;i >= 1;i--)
  {
    fac=fac*i;
  }
}

void main()
{
 

  print("Enter a num :");
  var num = int.parse(stdin.readLineSync().toString());

int fact = factorial(num);
print("factorial : $fact");
}