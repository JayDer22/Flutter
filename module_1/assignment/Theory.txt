1.)Explain fundamental data tyes in dart (int,double,String,List,Map,etc....)and their uses.

Ans. ->  1) Int :- Intiger is use for numbers.Integer is whole numbers (no decimal and no bianiry),it can be nagative or positive.
            ex  :  int a = 12;

         2) Double :- Double stores number with decimal point value.
            ex  :  double b = 1.14;

         3) String :- It stores values of character.
            ex  :  String c = "jay";

         4) List  :- List is a collection of items.
            ex  :  List<int> num = [1,2,3,4,5];

2.)Describe Control function in dart with example of if,else,for,while and switch.

Ans. -> Controal structure decid that your code when it run, how it run and how many times it will run.

       1) if,else :- if,else is an condition statement where you have to write your condition which you have to run in code.
           ex  : 
                 void main()
			{
 	                     int a = 2;
			     int b = 5;
                             if(a==b)
					{
					  print("A is equql to B");	
					}
			     else
                                 {
                                  print("A is not equal to B");
                                  }

 	
			}  
           output : A is not equal to B

       2) for loop :- for loop is use for repitation when you have to reapet the same process you can use for loop.
                      for loop is entry controal loop.
             ex : 
                    void main()
  				{
  					for(int i = 1;i <= 5;i++)
					{
 					 print(i);
					}
				}   
                         output : 1
        			  2
				  3
				  4
				  5

                            
       3) while loop :- It check contition befor execuing the loop.
            ex : 
                  void main()
			{
				int i = 1;
      				while(i <= 5)
      				 {
				   print(i);
				   i++;
				 }
			} 
                    output : 1
                             2
			     3
		             4
			     5

     4) Switch :-  switch statement is used to execute different blocks of code based on the value of a variable.

                         void main() {
                             var day = 3;

 				 switch (day) {
   					 case 1:
    					 print("Monday");
      					 break;
                 			 case 2:
      					 print("Tuesday");
     					 break;
    					 case 3:
     					 print("Wednesday");
     					 break;
   					 default:
     					 print("Invalid day");
 						 }
}   						 }